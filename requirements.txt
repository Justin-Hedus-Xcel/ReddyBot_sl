# Core app
streamlit==1.39.0

# HTTP requests and streaming
requests==2.32.3

# OAuth support
requests-oauthlib==1.3.1
oauthlib==3.2.2

# Optional: JSON handling (usually included in Python stdlib)
# json is built-in, so no need to add explicitly

# If you plan to use pandas for any data manipulation
pandas==2.2.3